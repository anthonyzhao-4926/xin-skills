# 风格参考图（非布局模板）

共 7 张 PNG，随 skill 发布。用途：**对齐线条、配色、字体、图标气质**；不要照搬图中的分栏、步骤或构图。

说明见 [references/style-reference.md](../../references/style-reference.md)。

从仓库根 `local/` 更新后：

```bash
cp local/*.png draw-style-pastel-sketchnote/assets/samples/
```
